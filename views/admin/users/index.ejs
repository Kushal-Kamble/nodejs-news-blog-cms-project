  <div id="admin-content">
      <div class="container">
          <div class="row">
              <div class="col-md-10">
                <h1 class="admin-heading"><i class="fa fa-users"></i> All Users</h1>
              </div>
              <div class="col-md-2">
                <a class="add-new" href="/admin/add-user">add user</a>
              </div>
              <div class="col-md-12">
                <div id="usersTable"></div>
              </div>
          </div>
      </div>
  </div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://unpkg.com/tabulator-tables@6.0.0/dist/js/tabulator.min.js"></script>

<script>
const tableData = <%- JSON.stringify(users) %>  // json formate  me convert kr dega

// console.log(tableData);

var table = new Tabulator("#usersTable", {
    data: tableData,
    layout:"fitColumns",      
    pagination:true,
    paginationSize:10,
    paginationSizeSelector:[5,10,25,50,100],
    columns: [
        { title: "S.No.", formatter:"rownum", width:100, hozAlign:"center" },
        { title: "Full Name", field: "fullname", headerFilter:"input" },
        { title: "User Name", field: "username", headerFilter:"input" },
        { title: "Role", field: "role", headerFilter:"input" },
        { title: "Actions", formatter: function(cell, formatterParams, onRendered) {
            return `<a href="/admin/update-user/${cell.getData()._id}" class="btn btn-sm btn-success">Edit</a>
                    <button class="btn btn-sm btn-danger delete-user" onclick="deleteUser('${cell.getData()._id}')">Delete</button>`;
        }}
    ],
});

// Delete User
async function deleteUser(id){
  // PUT THIS CODE IN TRY AND CATCH METHOD WITH CONFIRMANTION
  try {
    if (confirm("Are you sure you want to delete this user?")) {
      const response = await fetch(`/admin/delete-user/${id}`, { // ye id url me jayegi
        method: 'delete' // method delete hoga
      });
      if(response.ok){ // agar response ok hai to
        window.location.reload();// page ko reload kr dena
      }

      if(response.status == 400){// agar status 400 hai to
        const data = await response.json();// json me convert kr dena
        alert(data.message);// aur alert me message show kr dena
      }
      
    }
  } catch (error) {// agar error aata hai to
    console.log(error);// error ko console me show kr dena
  }
}
</script>

<%- contentFor('tabulatorCSS') %>
<link href="https://unpkg.com/tabulator-tables@6.0.0/dist/css/tabulator.min.css" rel="stylesheet">